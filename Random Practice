#include <iostream>
#include <stdlib.h>

using namespace std;

int main() {
    int x;
    srand(time(NULL));
    while(x != 1)
    {
      x = rand()%4+1;
      cout << x << endl;
    }
   return 0; 
}
