#include <iostream>
#include <vector>

using namespace std;

int main() 
{
 	vector<int> v;
	int key = 6;
  v.push_back(1);
  v.push_back(2);
  v.push_back(3);
  v.push_back(4);
  v.push_back(5);
  v.push_back(6);
  v.push_back(7);
  v.push_back(8);
  v.push_back(9);

	if (v.begin(), v.end(), key+1)
		cout << "Element found";
	else
		std::cout << "Element not found";
}
